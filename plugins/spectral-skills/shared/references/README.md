# Shared References

This directory stores cross-skill background knowledge.

Keep reader-specific file quirks, unusual CSV structures, table-header
judgment, preview behavior, and `read_plan` examples in
`skills/spectral-reader/references/` when the reader skill is implemented.

Shared references should explain concepts that affect multiple skills.
